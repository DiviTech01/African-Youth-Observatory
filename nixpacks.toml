[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd apps/api && npm install --legacy-peer-deps && npx prisma generate && npm run build"]

[start]
cmd = "cd apps/api && node dist/main.js"
